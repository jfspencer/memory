import React from 'react';
import ReactDOM from 'react-dom';
import { Card } from './Card';

describe('CardRow', () => {
    it('should map  over row prop and generate a list of Cards components', () => {

    });
})