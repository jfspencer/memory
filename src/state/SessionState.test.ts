import React from 'react';
import ReactDOM from 'react-dom';

describe('SessionState', () => {
    it('getGameLayout should return the boardConfig from state', () => {

    });

    it('SessionStateReducer should handle RESET action without user defined prefs', () => {

    });

    it('SessionStateReducer should handle RESET action with user defined prefs', () => {

    });

    it('SessionStateReducer TAP should append to turn when turn is < 2', () => {

    });

    it('SessionStateReducer TAP should eval playTurn for match, reset if not found', () => {

    });

    it('SessionStateReducer TAP should eval playTurn for match, update boardConfig if found', () => {

    });

    it('SessionStateReducer TAP should eval playTurn for match, updates player turn with action on match', () => {

    });

    it('SessionStateReducer TAP should reset turn if match is not found', () => {

    });
})